import 'package:flutter/material.dart';

const double maxPageWidth = 270.0;

const primaryColorApp = Color(0xFF0086B9);

const shadowApp = [
  BoxShadow(
    color: Colors.black26,
    offset: Offset(1.1, 1.1),
    blurRadius: 5.0,
  ),
];

const api = 'https://www.metaweather.com/api/location/';
